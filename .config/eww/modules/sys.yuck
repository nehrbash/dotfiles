(defwidget sys []
  (box
    :class "module"
    :space-evenly false
    (button
        :class "icon"
        :onclick "${EWW_CMD} open --toggle system-menu-closer --screen ${monitor.id}; ${EWW_CMD} open --toggle  system-menu --screen ${monitor.id}"
        "")))
