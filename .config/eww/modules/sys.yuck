(defwidget sys []
  (box
    :class "module"
    :space-evenly false
    (button
        :class "song-cover-art"
        :style "background-image: url(\"${profile-image}\");"
        :onclick "${EWW_CMD} open --toggle system-menu-closer --screen ${monitor.name}; ${EWW_CMD} open --toggle  system-menu --screen ${monitor.name}")))
