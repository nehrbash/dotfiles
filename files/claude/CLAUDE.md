# Global CLAUDE.md

## Documentation Workflow

- Before exploring unfamiliar code, check `~/doc/projects/<name>/index.org` for existing notes.
- After significant exploration (5+ files), persist findings to `~/doc/projects/<name>/index.org`.
- Use Org-mode markup in `~/doc/` (`*` headings, `[[target][label]]` links). No `:ID:` properties.
- Auto-memory is for Claude Code preferences only — real knowledge goes to `~/doc/`.
